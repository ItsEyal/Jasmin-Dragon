import { DEFAULT_THEME } from '@mantine/core';

export const jasmineDragonTheme = {
  ...DEFAULT_THEME,
  colors: {
    ...DEFAULT_THEME.colors,
    // Update the color palette based on your preferences
    // Here's an example with warm and earthy tones
    primary: '#7F5F3A',
    secondary: '#C8A86F',
    // ...
  },
  fontFamily: {
    // Update the font family as per your design requirements
    sans: 'Arial, sans-serif',
  },
  // Update the rest of the theme options as needed
  // For example, typography, spacing, breakpoints, etc.
};